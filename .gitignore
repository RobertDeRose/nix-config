# Nix build results
result
result-*
.hm-result
.hk.local.pkl

# mdBook build output
book/

# Don't ignore flake.lock — it's what makes the config reproducible!
# flake.lock

# macOS
.DS_Store

# Local Python and task build artifacts
__pycache__/
*.pyc
.build/
