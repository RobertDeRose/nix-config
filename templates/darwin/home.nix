{ pkgs, ... }:
{
  # Host-specific macOS home-manager overrides.
  # This file is imported automatically when present beside default.nix.
  #
  # Per-host user packages (merged with the global list in home/common/core.nix):
  #   home.packages = with pkgs; [ ... ];
}
