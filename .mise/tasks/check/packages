#!/usr/bin/env bash
# [MISE] description="Validate package inventory and ownership"

set -euo pipefail

TASK_FILE="${BASH_SOURCE[0]}"
REPO_ROOT="$(git -C "$(dirname "$TASK_FILE")" rev-parse --show-toplevel)"
cd "$REPO_ROOT"

mise run package:validate
