#!/usr/bin/env bash
# [MISE] description="Deprecated: forward to apply --debug"
# [MISE] hide=true

set -euo pipefail

TASK_FILE="${BASH_SOURCE[0]}"
REPO_ROOT="$(git -C "$(dirname "$TASK_FILE")" rev-parse --show-toplevel)"
cd "$REPO_ROOT"
exec mise run apply -- --debug "$@"
