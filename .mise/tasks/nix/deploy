#!/usr/bin/env bash
# [MISE] description="Deprecated: forward to deploy"
# [MISE] hide=true
#
#USAGE arg "<destination>" help="SSH destination"
#USAGE arg "[host]" help="Inventory host name"
#USAGE flag "-d --debug" help="Show verbose Nix output and traces"

set -euo pipefail
args=("${usage_destination}")
[ -z "${usage_host:-}" ] || args+=("${usage_host}")
[ "${usage_debug:-false}" != true ] || args+=(--debug)
exec mise run deploy -- "${args[@]}"
