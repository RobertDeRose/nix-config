#!/usr/bin/env bash
# [MISE] description="Deprecated: forward to apply"
# [MISE] hide=true
#
#USAGE flag "-d --debug" help="Show verbose Nix output and traces"
#USAGE flag "--host <host>" help="Host to apply"
#USAGE flag "--no-activate" help="Build without activation"

set -euo pipefail
exec mise run apply -- "$@"
