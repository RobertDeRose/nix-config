#!/usr/bin/env bash
# [MISE] description="Deprecated: forward to update"
# [MISE] hide=true
#
#USAGE arg "[input]" help="Optional flake input to update"

set -euo pipefail
args=()
[ -z "${usage_input:-}" ] || args+=("${usage_input}")
exec mise run update -- "${args[@]}"
