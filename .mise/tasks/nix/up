#!/usr/bin/env bash
# [MISE] description="Deprecated: forward to update"
# [MISE] hide=true
#
#USAGE arg "[input]" help="Optional flake input to update"

set -euo pipefail

TASK_FILE="${BASH_SOURCE[0]}"
REPO_ROOT="$(git -C "$(dirname "$TASK_FILE")" rev-parse --show-toplevel)"
cd "$REPO_ROOT"
exec mise run update -- "$@"
