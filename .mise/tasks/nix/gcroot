#!/usr/bin/env bash
# [MISE] description="Show auto GC roots in the nix store"
# [MISE] quiet=true

set -euo pipefail

TASK_FILE="${BASH_SOURCE[0]}"
REPO_ROOT="$(git -C "$(dirname "$TASK_FILE")" rev-parse --show-toplevel)"
cd "$REPO_ROOT"

ls -al /nix/var/nix/gcroots/auto/
