# schema
[settings]
env_shell_expand = true
jobs = 1

[env]
HK_MISE = 1
HK_PKL_BACKEND = "pklr"


# ---------------------------------------------------------------------------
# Bootstrap & host management
# ---------------------------------------------------------------------------

[tasks."nix:init"]
description = "Full init for a new machine: install Nix (+ Homebrew on Mac), then apply config"
usage = '''
arg "<hostname>" {
  help "Hostname to assign to this machine"
  long_help """
  The hostname used as the nix config identifier.
  Delegates to nix:add-host to create the host directory, then installs
  prerequisites (Nix, Homebrew) and builds/activates the configuration.
  """
}
'''

[tasks.install-nix]
description = "Install Nix via nix-installer if missing"
hide = true
quiet = true
outputs = "/nix/receipt.json"
run = '''
#!/usr/bin/env bash
set -euo pipefail

HOSTNAME="${usage_hostname:?}"
OS="$(uname -s)"

# ------------------------------------------------------------------ #
# Create host directory (delegates to nix:add-host)
# ------------------------------------------------------------------ #
mise run nix:add-host "$HOSTNAME"

# Stage everything before the nix build
git checkout -b "host/$HOSTNAME" 2>/dev/null || true
git add -A

# ------------------------------------------------------------------ #
# macOS: install Homebrew if missing
# ------------------------------------------------------------------ #
if [ "$OS" = "Darwin" ]; then
  echo "==> Checking for Homebrew..."
  if ! command -v brew &>/dev/null; then
    echo "    Installing Homebrew..."
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    echo >> ~/.zprofile
    echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
    eval "$(/opt/homebrew/bin/brew shellenv)"
  else
    echo "    Homebrew already installed, skipping."
  fi
fi

# ------------------------------------------------------------------ #
# Install Nix if missing
# ------------------------------------------------------------------ #
echo "==> Checking for Nix..."
if [ ! -e /nix/receipt.json ]; then
  echo "    Installing Nix via nix-installer..."
  curl -sSfL https://artifacts.nixos.org/nix-installer | sh -s -- install
else
  curl -sSfL https://install.lix.systems/lix | bash -s -- install
fi
'''

[tasks.add-host]
description = "Register a new host by creating its hosts/ directory (no flake.nix editing needed)"
hide = true
quiet = true
usage = '''
arg "[hostname]" help="Hostname for the new machine" \
  long_help="Used as both the nix config identifier and the machine hostname. Creates a hosts/<arch>-<os>/<hostname>/ directory from the appropriate template. easy-hosts auto-discovers hosts from the directory structure."
arg "[os]" help="OS class (default: current machine)" {
  choices "darwin" "linux"
}
arg "[arch]" help="Architecture for the new machine (default: current machine)" long_help="Used to determine the host directory path: <arch>-darwin or <arch>-linux. Inferred from the current machine if omitted." {
  choices "aarch64" "x86_64"
}
'''
run = '''
#!/usr/bin/env bash
set -xeuo pipefail

# ------------------------------------------------------------------ #
# Build and activate
# ------------------------------------------------------------------ #
if [ "$OS" = "Darwin" ]; then
  echo "==> Building and activating Darwin configuration for '$HOSTNAME'..."
  nix build ".#darwinConfigurations.$HOSTNAME.system" \\
    --extra-experimental-features 'nix-command flakes'
  sudo -E ./result/sw/bin/darwin-rebuild switch --flake ".#$HOSTNAME"
else
  echo "==> Building and activating NixOS configuration for '$HOSTNAME'..."
  sudo nixos-rebuild switch --flake ".#$HOSTNAME" \\
    --extra-experimental-features 'nix-command flakes'
fi

SYSTEM="${ARCH}-${OS}"
HOST_DIR="hosts/${SYSTEM}/${HOSTNAME}"

[tasks."nix:switch"]
description = "Apply the nix config for the current host (auto-detects Darwin or NixOS)"
run = """
#!/usr/bin/env bash
set -xeuo pipefail

if [ -n "${GITHUB_TOKEN:-}" ]; then
  export NIX_CONFIG="${NIX_CONFIG:-}"$'\n'"access-tokens = github.com=${GITHUB_TOKEN}"
fi

set +u
source /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
set -u

HOSTNAME="${usage_hostname:-$(hostname -s)}"

# Define the target based on OS
if [ "$(uname -s)" = "Darwin" ]; then
  darwin-rebuild switch --flake ".#$HOST"
else
  sudo nixos-rebuild switch --flake ".#$HOST"
fi

[tasks."nix:debug"]
description = "Apply the nix config with verbose output for debugging"
run = """
#!/usr/bin/env bash
set -euo pipefail

if [ -n "${GITHUB_TOKEN:-}" ]; then
  export NIX_CONFIG="${NIX_CONFIG:-}"$'\n'"access-tokens = github.com=${GITHUB_TOKEN}"
fi

set +u
source /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
set -u

HOSTNAME="${usage_hostname:-$(hostname -s)}"
if [ "$(uname -s)" = "Darwin" ]; then
  darwin-rebuild switch --flake ".#$HOST" --show-trace --verbose
else
  sudo nixos-rebuild switch --flake ".#$HOST" --show-trace --verbose
fi
"""

[tasks."nix:add-host"]
description = "Register a new host by creating its hosts/ directory (no flake.nix editing needed)"
usage = '''
arg "<hostname>" {
  help "Hostname for the new machine"
  long_help """
  Used as both the nix config identifier and the machine's actual hostname.
  Creates a hosts/<arch>-<class>/<hostname>/ directory from the appropriate template.
  easy-hosts auto-discovers hosts from the directory structure — no flake.nix editing needed.
  """
}
arg "[system]" {
  help "Nix system string (default: current machine's system)"
  long_help """
  Determines the host directory path: <arch>-darwin or <arch>-nixos.
  Inferred from the current machine if omitted.
  """
  choices "aarch64-darwin" "x86_64-darwin" "x86_64-linux" "aarch64-linux"
}
'''
run = '''
#!/usr/bin/env bash
set -euo pipefail

echo ""
echo "Done! Open a new shell to pick up all changes."
'''

[tasks.switch]
description = "Apply the nix config for the current host (auto-detects Darwin or Linux)"
usage = '''
flag "-d --debug" help="Show verbose output and nix traces for debugging"
'''
run = '''
#!/usr/bin/env bash
set -euo pipefail
HOST="$(hostname -s)"

# Map system string to easy-hosts directory convention
case "$SYSTEM" in
  aarch64-darwin) DIR_PREFIX="aarch64-darwin"; TEMPLATE="darwin" ;;
  x86_64-darwin)  DIR_PREFIX="x86_64-darwin";  TEMPLATE="darwin" ;;
  x86_64-linux)   DIR_PREFIX="x86_64-nixos";   TEMPLATE="nixos"  ;;
  aarch64-linux)  DIR_PREFIX="aarch64-nixos";   TEMPLATE="nixos"  ;;
  *) echo "ERROR: Unknown system '$SYSTEM'"; exit 1 ;;
esac

HOST_DIR="hosts/${DIR_PREFIX}/${HOSTNAME}"
echo "==> Adding $HOSTNAME ($SYSTEM) -> $HOST_DIR"

# Create host directory from appropriate template
if [ -d "$HOST_DIR" ]; then
  echo "WARNING: $HOST_DIR already exists, skipping directory creation."
else
  echo "==> Creating $HOST_DIR from templates/$TEMPLATE..."
  mkdir -p "$HOST_DIR"
  cp -r "templates/$TEMPLATE"/* "$HOST_DIR"/
fi

# On NixOS, auto-copy hardware-configuration.nix if running on the target machine
if [ "$TEMPLATE" = "nixos" ] && [ ! -e "$HOST_DIR/hardware-configuration.nix" ]; then
  if [ -e /etc/nixos/hardware-configuration.nix ]; then
    echo "==> Copying /etc/nixos/hardware-configuration.nix into $HOST_DIR..."
    cp /etc/nixos/hardware-configuration.nix "$HOST_DIR/"
  else
    echo "==> Generating hardware configuration..."
    sudo nixos-generate-config --dir "$HOST_DIR"
    # nixos-generate-config also creates configuration.nix which we don't need
    rm -f "$HOST_DIR/configuration.nix"
  fi
fi

git add -A
git commit -m "chore: add host $HOSTNAME ($SYSTEM)" 2>/dev/null || true

if [ "$(uname -s)" = "Darwin" ]; then
  # "${VERBOSE_ARGS[@]}" expands to nothing if the array is empty
  sudo darwin-rebuild switch --flake ".#$HOST" "${VERBOSE_ARGS[@]}"
else
  echo "   Next steps on the target machine:"
  echo "   1. Ensure $HOST_DIR/hardware-configuration.nix is present"
  echo "   2. ./bootstrap.sh $HOSTNAME"
fi
'''

[tasks.debug]
description = "Apply config with debug traces"
run = "mise run switch --debug"

# ---------------------------------------------------------------------------
# Nix maintenance tasks
# ---------------------------------------------------------------------------

[tasks."nix:up"]
description = "Update all flake inputs"
run = "nix flake update"

[tasks."nix:upp"]
description = "Update a single flake input"
usage = 'arg "<input>" help="Flake input to update (e.g. nixpkgs, home-manager, darwin)"'
run = "nix flake update ${usage_input:?}"

[tasks."nix:history"]
description = "List all system profile generations"
run = "nix profile history --profile /nix/var/nix/profiles/system"

[tasks."nix:repl"]
description = "Open a nix repl with nixpkgs"
run = "nix repl --expr 'import (builtins.getFlake \"nixpkgs\") {}'"

[tasks."nix:clean"]
description = "Remove all profile generations older than 7 days"
run = "sudo nix profile wipe-history --profile /nix/var/nix/profiles/system --older-than 7d"

[tasks."nix:gc"]
description = "Garbage-collect all unused nix store entries (system + user)"
run = [
  "sudo nix-collect-garbage --delete-older-than 7d",
  "nix-collect-garbage --delete-older-than 7d",
]

[tasks."nix:fmt"]
description = "Format all .nix files in this repo"
run = "nix fmt"

[tasks."nix:iterm-export"]
description = "Re-export iTerm2 preferences plist into the repo"
run = """
#!/usr/bin/env bash
set -euo pipefail

SRC="$HOME/Library/Preferences/com.googlecode.iterm2.plist"
DST="config/iterm2/com.googlecode.iterm2.plist"

if [ ! -e "$SRC" ]; then
  echo "ERROR: iTerm2 preferences not found at $SRC"
  exit 1
fi

echo "==> Copying iTerm2 plist into repo..."
cp "$SRC" "$DST"
echo "    Exported to $DST"
echo ""
echo "Review with: git diff --stat"
echo "Then commit: git add -A && git commit -m 'chore: update iTerm2 preferences'"
"""

[tasks."nix:gcroot"]
description = "Show auto GC roots in the nix store"
run = "ls -al /nix/var/nix/gcroots/auto/"

[tasks."nix:uninstall"]
description = "Completely uninstall Nix from this machine"
confirm = "This will completely remove Nix, the /nix store, and all managed packages. Continue?"
run = '''
#!/usr/bin/env bash
set -euo pipefail

echo "==> Starting uninstallation process..."

# 1. Uninstall Configuration Managers FIRST
if [[ "$(uname -s)" == "Darwin" ]]; then
    if command -v darwin-uninstaller &> /dev/null; then
        echo "--> Found nix-darwin, running uninstaller..."
        sudo darwin-uninstaller
    elif [[ -d "/etc/static" ]]; then
        # If the binary is missing but traces exist, try running from flake
        echo "--> nix-darwin detected but uninstaller not in PATH. Trying nix run..."
        sudo nix --extra-experimental-features "nix-command flakes" \
            run github:LnL7/nix-darwin#darwin-uninstaller || true
    fi
else
    # Linux: Check for system-manager (usually doesn't have a standalone uninstaller,
    # but we should at least acknowledge it)
    if [[ -d "/etc/system-manager" ]]; then
        echo "--> system-manager detected. Note: Manual cleanup of /etc/system-manager may be required."
    fi
fi

# 2. Uninstall Nix via nix-installer
if [[ -e /nix/nix-installer ]]; then
    echo "==> Uninstalling Nix via nix-installer..."
    sudo /nix/nix-installer uninstall
else
    echo "ERROR: /nix/nix-installer not found."
    echo "       Nix may not have been installed via the Determinate nix-installer."
    echo "       Refer to https://nix.dev for manual steps."
    exit 1
fi

echo -e "\nDone! Open a new shell or restart your computer to fully clear changes."
'''
